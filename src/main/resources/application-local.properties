# ── Profilo LOCAL (sviluppo) ─────────────────────────────────────────────────

# Ambiente esplicito (necessario per logback-spring.xml e LogUtil)
env=DEV

# Actuator: tutto esposto per debug locale
management.endpoints.web.exposure.include=*
management.endpoint.health.show-details=always

# Logging: testo leggibile (gestito da logback-spring.xml — DEV → ROOT=INFO)
